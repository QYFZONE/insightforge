ARK_API_KEY=
ARK_MODEL_ID=
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

HTTP_ADDR=:8080

# 可选值：
#   memory：内存存储，重启后数据丢失
#   sqlite：SQLite / GORM 持久化存储
STORE_DRIVER=sqlite
SQLITE_PATH=data/insightforge.db
